extends layout

block content
  h1 Employees
  table.table
    thead
      tr
        th ID
        th Name
        th Email
        th Active
    tbody
      each employee in employees
        tr
          td= employee.id
          td= employee.name
          td= employee.email
          td= employee.active ? 'Yes' : 'No'
